# piebus

piebus - self-hosted personal web-presence + chat assistant + automation platform.

# Install

```bash
pip install piebus
```


# Create "demosite"

```bash
mkdir demosite
cd demosite

piebus init .
```


# Serve "demosite"

```bash
cd demosite
piebus serve --debug
```
